{
  "name": "blockin-algo-driver",
  "version": "1.0.1",
  "description": "",
  "type": "module",
  "main": "dist/AlgoDriver.js",
  "scripts": {
    "prepack": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsc --build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "algosdk": "^1.16.0",
    "blockin": "^1.1.0",
    "buffer": "^6.0.3",
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "typescript": "^4.6.4"
  }
}
